spring:
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 50MB
  mvc:
    contentnegotiation:
      default-content-type: application/json
springdoc:
  default-produces-media-type: application/json


server:
  port: 8088
  servlet:
    context-path: /api/v1/